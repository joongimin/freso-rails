UIUtil.prototype.slide_content("<%=j render("guide") %>");
$("div.parallax").each(function() {
  if ($(this).attr("id") != "home") {
    $(this).hide();
  }
});
$("#nav").fadeOut();
<%= render("shared/history") %>