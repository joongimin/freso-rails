<% @brand = Brand.new %>
<% render(:template => "brands/new", :formats => [:html], :layout => "layouts/application_login") %>
freso.ui_util.slide_content($("ul#body_slider"), "<%=j content_for :body %>");
$("div.parallax").each(function() {
  if ($(this).attr("id") != "home") {
    $(this).hide();
  }
});
$("#nav").fadeOut();
<%= render("shared/history", :controller_path => "brand", :action_name => "new") %>